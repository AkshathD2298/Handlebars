<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Table with Color Change</title>
    <style>
        table {
            border-collapse: collapse;
            width: 100%;
        }
        th, td {
            border: 1px solid black;
            padding: 8px;
            text-align: left;
        }
        tr.empty-avg-reviews {
            background-color: lightcoral; 
        }
    </style>
</head>
<body>


    <h2>Books</h2>

    <table>
        <thead>
            <tr>
                 <th>Title</th>
                <th>Author</th>
                <th>Price</th>
                <th>pages</th>
                <th>Average Reviews</th>
            </tr>
        </thead>
        <tbody>
            {{#each data}}
                <tr {{#unless this.avg_reviews }}bgcolor="lightcoral"{{/unless}}>
                    <td>{{this.title}}</td>
                    <td>{{this.author}}</td>
                    <td>{{this.price}}</td>
                    <td>{{this.pages}}</td>
                    <td>{{this.avg_reviews}}</td>
                </tr>
            {{/each}}
        </tbody>
    </table>
        {{> table}}
</body>
</html>
